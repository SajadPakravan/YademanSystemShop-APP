import 'package:flutter/material.dart';

class LoggedView extends StatelessWidget {
  const LoggedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
